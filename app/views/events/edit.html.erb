<h1>edit event for <%= @location.name %></h1>

<div class="grid_7 left_align">
	<% form_for([@location, @event]) do |f| %>
		<%= render :partial => 'form', :object => f -%>
	<% end %>
</div>

<%= link_to 'Show', @event %> |
<%= link_to 'Back', events_path %>